version: '3.9'

services:
  app:
    image: origin-service-app:latest
    container_name: origin-service-app
    build:
      context: ./
      args:
        - JAR_FILE=build/libs/\*.jar
    tty: true
    stdin_open: true
    environment:
      - SPRING_PROFILES_ACTIVE=local
    networks:
      - poc-ir-bra_local-network
    ports:
      - "5000:5000"

networks:
  poc-ir-bra_local-network:
    external: true